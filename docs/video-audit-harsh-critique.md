# Agent IC Final Demo - Harsh Current Critique

**Artifact:** `demo-out/agent-ic-demo-final.mp4`  
**Duration:** 73.13 s  
**Frame count:** 2,194  
**Current deterministic verdict:** PASS  
**Current ChatGPT 5.5 review verdict:** PASS, 50/50

## Critical Finding

No current blocker remains in the rendered MP4 or current proof artifacts.

ChatGPT 5.5 passed 20 specialized reviews, 20 overarching-goal reviews, and 10 product-specific reviews.

## What Is Fixed

- The cost comparison supports the product thesis: `$3,036` human queue cost vs `$532` governed Agent IC cost.
- The final QR decodes to `https://github.com/agent-ic`.
- The rendered video has no visible local URLs, private paths, raw cents copy, full provider IDs, or stale repo wording in deterministic OCR.
- The sidecar no longer exposes internal audit IDs that OCR can confuse with a port.
- Voiceover and captions are generated from the current script and align with stage progress.
- Voiceover now uses Edge neural TTS instead of the earlier local robotic voice.
- NHTSA evidence is supported by source URL, row preview, and artifact hash.
- The CARFAX block is shown as a worker-agent policy request with HTTP 403 evidence.
- Stripe is framed as test mode, not production money movement.
- ChatGPT 5.5 adversarial final readiness passed.

## Harsh Product Read

The current video reads as an enterprise control plane for governed service trials: a buyer tests a worker-agent against public NHTSA workload rows, Agent IC gates spend and tool access, blocks an out-of-policy paid request, measures evidence, and decides controlled expansion.

The strongest proof points are the event-driven stage timing, live Nemotron receipt, test-mode Stripe receipt, external policy 403, visible evidence source/hash, and the productivity comparison. The proof cards carry the trust burden without relying on local/private context.

## Submission-Risk Summary

Known deterministic risks are closed. ChatGPT 5.5 review coverage is complete and green. The final edit handoff is now mcp-video based, open source, and runnable in this Linux validation environment. Re-rendering the video would invalidate this current-state audit until all gates are rerun.
